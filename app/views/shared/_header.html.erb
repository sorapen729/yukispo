<!-- ヘッダー -->
<header class="w-full bg-sky-400 text-white">
  <div class="max-w-md md:max-w-2xl lg:max-w-4xl mx-auto flex items-center justify-between px-4 md:px-8 lg:px-12 py-3">
    <%= link_to '/', class: "text-xl md:text-2xl lg:text-3xl font-bold tracking-wide flex items-center gap-1" do %>
      ゆきスポ<span aria-hidden="true">❄️</span>
    <% end %>

    <div class="flex items-center gap-4 md:gap-6">
      <% if user_signed_in? %>
        <span class="text-sm md:text-base underline">
          <%= "#{current_user.last_name || '姓なし'} #{current_user.first_name || '名なし'}" %>
        </span>
        <%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-sm md:text-base hover:underline" %>
      <% else %>
        <%= link_to "ログイン", new_user_session_path, class: "text-sm md:text-base hover:underline" %>
      <% end %>

      <!-- ハンバーガー -->
      <button id="hamburger-btn" class="hamburger-btn" aria-label="メニューを開く" aria-controls="hamburger-nav" aria-expanded="false">
        <span></span>
        <span></span>
        <span></span>
      </button>

      <nav id="hamburger-nav" class="hamburger-nav" aria-hidden="true">
        <ul>
          <li><%= link_to '❓クイズ一覧', "#" %></li>
          <% if user_signed_in? %>
            <li><%= link_to '✏️クイズを作る', "#" %></li>
            <li><%= link_to '👤マイページ', profile_path(current_user) %></li>
            <hr>
            <li>
              <%= link_to '🎈ログアウト', "#", data: { "turbo-method": :delete, turbo_confirm: "ログアウトしてもよろしいですか？" } %>
            </li>
          <% else %>
            <li><%= link_to '🚪ログイン', "#" %></li>
            <li><%= link_to '📝ユーザー登録', "#" %></li>
            <hr>
            <li>
              <a href="#" class="js-open-login-modal" title="クイズを作るにはログインが必要です">✏️クイズを作る</a>
            </li>
          <% end %>
          <li><%= link_to '📘遊び方', "#" %></li>
        </ul>
      </nav>

      <div id="overlay" class="overlay"></div>
    </div>
  </div>
</header>